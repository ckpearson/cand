<UserControl x:Class="CandidateAssessment.Views.ImportScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
        <TextBlock Text="Performing Import..." FontSize="18" />
        <ProgressBar Margin="0,10,0,0" Height="15" IsIndeterminate="True"
                     Value="{Binding Progress, Mode=OneWay}" />
        <TextBlock Text="{Binding Message, Mode=OneWay}" Margin="0,10,0,0" FontSize="14" TextWrapping="Wrap" TextAlignment="Center" />
    </StackPanel>
</UserControl>
